@model IEnumerable<Content_Management_System.Models.CategoryField>
@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="content-wrapper">
    <!-- Content Header (Page header) -->
    <section class="content-header">
        <h1>
            Category Fields
            <small>All Items</small>
        </h1>
    </section>

    <!-- Main content -->
    <section class="content">
        <div class="row">
            <div class="col-xs-12">
                <div class="box">
                    <div class="box-header">
                        <h3 class="box-title">@Html.ActionLink("Create New Item", "Save", "CategoryFields", new { @class = "btn btn-primary" })</h3>
                    </div>
                    <!-- /.box-header -->
                    <div class="box-body">
                        <table id="example2" class="table table-bordered table-hover">
                            <thead>
                                <tr>
                                    <th>Name</th>
                                    <th>Type</th>
                                    <th>Required</th>
                                    <th>Category</th>
                                    <th>Is Active</th>
                                    <th>Edit</th>
                                    <th>Delete</th>
                                </tr>
                            </thead>
                            <tbody>
                                @foreach (var categoryFieldItem in Model)
                                {
                                    <tr>
                                        <td>@categoryFieldItem.Name</td>
                                        <td>@categoryFieldItem.Type</td>
                                        <td>@categoryFieldItem.Requierd</td>
                                        <td>@categoryFieldItem.CategoryId</td>
                                        <td>@categoryFieldItem.IsActive</td>
                                        <td>@Html.ActionLink("Edit", "Edit", "CategoryFields", new { id = categoryFieldItem.Id }, "")</td>
                                        <td>@Html.ActionLink("Delete", "Delete", "CategoryFields", new { id = categoryFieldItem.Id }, "")</td>
                                    </tr>
                                }
                            </tbody>
                            <tfoot>
                                <tr>
                                    <th>Name</th>
                                    <th>Tpye</th>
                                    <th>Required</th>
                                    <th>Category</th>
                                    <th>Is Active</th>
                                    <th>Edit</th>
                                    <th>Delete</th>
                                </tr>
                            </tfoot>
                        </table>
                    </div>
                    <!-- /.box-body -->
                </div>
                <!-- /.box -->
            </div>
            <!-- /.col -->
        </div>
        <!-- /.row -->
    </section>
    <!-- /.content -->
</div>